services:
  ui:
    container_name: hybrid-crs-ui
    build:
      context: ./hybrid-crs-ui
      dockerfile: ./dev.Dockerfile
    ports:
      - 3001:3000
    volumes:
      - ./hybrid-crs-ui/src:/app/src
      - ./hybrid-crs-ui/public:/app/public
      - ./hybrid-crs-ui/.next:/app/.next
      - /app/node_modules
    tty: true
    stdin_open: true
    restart: unless-stopped
